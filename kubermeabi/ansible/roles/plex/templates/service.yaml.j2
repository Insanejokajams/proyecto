apiVersion: v1
kind: Service
metadata:
  name: {{ plex.name }}
  namespace: {{ plex.namespace }}
spec:
  type: ClusterIP
  ports:
    - port: {{ plex.port }}
      targetPort: {{ plex.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ plex.name }}
